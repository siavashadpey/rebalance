language: python


# only invoke travis-ci for certain branches
branches:
  only:
    - master
    - dev

before_script:
  - skip
  #pip install coverage==4.5.4
  #pip install python-coveralls

script:
  - Skip
  #./travis_run.sh

after_success: 
  - skip
  #coveralls

stages:
  - Testing
  - name: Upstream
    if: branch = master


jobs:
  include:
    - name: Testing
      stage: Testing
      before_script: 
        - pip install coverage==4.5.4
        - pip install python-coveralls
      script: 
        - ./travis_run.sh
      after_success:
        - coveralls
    - name: 
        - Upstream
      stage: 
        - Upstream
      install: 
        - skip
      script: echo "hello" # TODO: replace this by a script which pushes from dev to master
      after_success:
        - skip


notifications:
  email:
    recipients:
      - siavosh@ualberta.ca
      #- other@example.com
    on_success: never # default: change
    on_failure: always # default: always